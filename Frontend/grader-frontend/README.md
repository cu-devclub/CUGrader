This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

first, pls create `.env` file at this directory, see `.env.template` for references.

then, to start development server:

```bash
pnpm dev
# or
npm run dev
```

## Build
dockerfile is currently unusable as it was made to serve simple CRA app not nextjs. There is an ongoing attempt to update it, see `feat/frontend/dockerfile`.
